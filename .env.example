BOT_TOKEN=123456:ABC-your-telegram-bot-token
CHAT_ID=123456789
# Опционально: треки добавляются через ТГ (присылайте ссылку tracking.ozon.ru).
# Эта переменная только для первичного заполнения /data/tracks.json, если файл пуст.
TRACK_URLS=
# Дамп HTML/текста страницы в /data/debug при ошибках (0/1)
DEBUG_DUMP=0

# Опциональный прокси для Chromium — нужен, если сервер на нероссийском IP,
# а Ozon банит дата-центровые IP по antibot. Поддерживаются http://, https://, socks5://
# PROXY_SERVER=http://1.2.3.4:8080
# PROXY_USERNAME=
# PROXY_PASSWORD=

# --- Тюнинг лимитов (всё опционально, дефолты в скобках) ---
# Параллельных браузеров (1..5). На слабых VPS оставляйте 1 — Chromium прожорлив.
# CONCURRENCY=1
# Интервал авто-проверки (минимум/максимум, сек). По умолчанию 30..59 мин.
# POLL_MIN_SEC=1800
# POLL_MAX_SEC=3540
# Жёсткие таймауты на один fetch (внутренний / общий, сек).
# FETCH_TIMEOUT_SEC=150
# FETCH_HARD_TIMEOUT_SEC=180
